spring:
  datasource:
    url: jdbc:mysql://rds-endpoint.amazonaws.com:3306/upgradehub
    username: upgradehub_user
    password: Suv2103@

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
toss:
  secret-key: ${TOSS_SECRET_KEY}        